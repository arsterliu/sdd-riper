---
date: 2026-08-23
task-name: "gate-integrity-fixes"
mode: micro
autonomy-mode: "auto"
autonomy-mode-source: "discover-override"
status: archived
reopened-from: ""
context-source: ""
ui-impact: "no"
visual-context-intent: "not-applicable"
visual-evidence: ""
visual-evidence-file: ""
diff-base: "d741362fe808b1f4f78c9d771e5d6fe1d5f25606"
design-file: ""
execute-log-file: "mydocs/archive/v4.12-gate-integrity-fixes.execute.md"
learning-file: "mydocs/archive/v4.12-gate-integrity-fixes.learning.md"
project-profile-revision: ""
project-profile-digest: ""
affected-units: ""
---

<!-- Keep template headings and labels in English. Fill actual narrative content in Chinese. -->

## Summary

修复三个门禁命令的数据完整性缺陷：challenge 结果写入的替换注入、AC 编号宽度不对称导致的覆盖死锁、cruise --record-run 在 migration-required 项目上的守卫绕过。

## Intake

requirement: 1) challenge.js --record-result 用字符串参数调用 String.replace，summary 含 `$'`/`$&` 等模式序列时把 Spec 尾部整段拼进该行，静默损坏文件（src/commands/challenge.js:67-73）；且模板缺标签行时 replace 静默 no-op，证据未持久化仍报成功。2) AC 编号宽度不对称：Spec 声明解析用 AC-\d+（workflow-gate-facts.js acceptanceFacts），Execute Log 覆盖记录只认 AC-\d{3}（acCoverageRecords），写 AC-42 会声明成功但永远无覆盖证据，归档门禁死锁。3) migration-required（.sdd-config 缺 AUTONOMY_MODE）项目执行 sdd cruise --record-run 绕过 discover_spec 守卫，命中 cruise-run.js appendRun 防御性 throw 裸堆栈 exit 1（v4.11 Learning Record 记录的残留）。
goal: 1) 替换改函数形式杜绝模式注入，缺失标签行时显式报错；2) 两处 AC 正则统一为 \d{3}，非三位编号给出明确诊断；3) 无活跃 Spec 时 --record-run 与普通 cruise 一致走引导输出并正常退出。
constraints: 不改变有-Spec、合法输入下的任何现有行为；不引入新依赖；AC 正则以 \d{3} 为准（与现有全部测试/文档一致）。

<!-- Micro skips Research, Innovate, and standalone Design. -->

## Plan
<!-- Required labels below stay English; fill values in Chinese where possible. -->

Required fields:
Impact Scope: src/commands/challenge.js（--record-result 写入逻辑）、src/core/workflow-gate-facts.js（acCoverageRecords 的 AC 正则）、src/commands/cruise.js（--record-run 无-Spec 守卫）；tests/workflow.test.js 与 tests/challenge.test.js（新增回归测试，以现有测试文件归属为准）。
Data Impact: 不改变任何数据文件结构；challenge --record-result 修复后写入内容更可靠（不再被模式序列破坏）。
Interface Impact: 1) `sdd challenge --record-result --summary "含 $' 文本"` 不再损坏 Spec，摘要原样落盘；模板缺 Challenge 标签行时命令报错退出而非假成功。2) Spec 声明或 Execute Log 记录中出现 AC-\d{1,2}/\d{4,} 时给出明确 blocker 指引（统一为三位），不再静默死锁。3) migration-required 项目 `sdd cruise --record-run` 输出引导并 exit 0，不再裸堆栈。合法三位编号与有-Spec 路径行为全部不变。
Acceptance:
- AC-001 challenge --record-result 的 summary 含 $'/$&/$` 等模式序列时，Spec 中 Challenge Summary 行与原文完全一致，无尾部拼接、无文件损坏。
  Verification: unit
  Automated: yes
  Test: tests/workflow.test.js
- AC-002 Spec 模板缺少任一 Challenge 标签行时执行 --record-result 以非零退出并输出缺失标签诊断，不写文件。
  Verification: unit
  Automated: yes
  Test: tests/workflow.test.js
- AC-003 AC 编号非三位（如 AC-42）在声明侧或覆盖记录侧产生明确 blocker（提示统一为 AC-### 三位格式），不再出现「声明成功但覆盖永远缺失」的静默死锁。
  Verification: unit
  Automated: yes
  Test: tests/workflow.test.js
- AC-004 migration-required 项目执行 sdd cruise --record-run 输出与普通 cruise 一致的引导（NEXT_ACTION: migrate_autonomy_config）并 exit 0。
  Verification: unit
  Automated: yes
  Test: tests/workflow.test.js
Verification:
Conditional field:
Provider: <required for e2e; named provider id>

Recommended fields:
Selected Option: 三个修复各自最小化：1) challenge.js 六个 .replace 全部改函数替换 function(m){ return label + ': ' + value; } 并在替换前校验六个标签行都存在（用 frontmatter 式 /^Label:/m 探测），缺则列出缺失标签 exit 1。2) acCoverageRecords 正则放宽为 (AC-\d{1,6}) 后按非三位判定：在 acCoverageContractIssues / validateAcCoverage 对声明 id 与记录 id 做「非三位」检查并产出 blocker「AC id must be zero-padded three digits: AC-42 -> AC-042」？——不引入自动补零转换，仅诊断，因为自动改写会篡改用户工件；同时 acceptanceFacts 声明解析保持 \d+ 但在 acceptance issues 里对非三位声明追加同一诊断。3) cruise.js 守卫条件从 nextAction === 'discover_spec' 扩为 (discover_spec || migrate_autonomy_config) 且 state.specPath 为空时同样走 CRUISE UNAVAILABLE 引导（文案按 nextAction 区分 discover/migrate 提示），--record-run 在 return 之前自然跳过。
Scope: 仅修三处缺陷路径；不改 appendRun 本身（其防御性 throw 作为最后防线保留）；不动 acceptanceFacts 的 \d+ 解析主逻辑，只追加诊断。
Touched Files: src/commands/challenge.js, src/core/workflow-gate-facts.js, src/commands/cruise.js, tests/workflow.test.js
Change: 见 Selected Option 三点；测试分别断言注入防护、缺失标签报错、AC-42 诊断、migration-required + record-run exit 0。
Blast Radius: challenge 写入路径所有调用方受益于函数替换（合法输入行为不变）；workflow-gate-facts 新增诊断只对非三位编号生效，现有全三位测试不受影响；cruise 守卫扩展仅覆盖 specPath 为空的分支，有-Spec 输出不变。

Plan Approved By: agent:main
Approved At: 2026-08-23T04:54:10.639Z
Gate Evidence: three minimal fixes validated by sdd validate (only approval/execute-log blockers remain); AC-001..AC-004 unit-verifiable; blast radius limited to defect paths, legal-input behavior unchanged.
## Execute Log Reference
execute-log-file: "mydocs/archive/v4.12-gate-integrity-fixes.execute.md"

## Completion Verification
<!-- AC Coverage records belong to a formal execution Step's `AC Coverage:` section. The `completion-verification` records only the four-axis self-check, result, and verification; Summary is not evidence. This replaces the former Review Summary section. -->

Challenge Verdict: PASS_WITH_CONCERNS
Backtrack Target: Learning Check
Challenge Summary: 独立子代理审核通过：git diff 证实改动限于声明的五文件+测试文件且与 Plan/Execute Log 一致（Step 4 BUGFIX_ESCALATED 偏差描述与代码逐字吻合）；node --test tests/commands.test.js 162 pass 全绿、npm test 721 tests/716 pass/0 fail；临时 fixture 实测 executedBy 注入逐字落盘、缺标签 exit 1 不写文件、cruise 三路径（legacy 有-Spec 保账本/无-Spec discover 引导/migration-required 引导不建账本）全部符合预期；两处平行实现诊断消息一致且有去重。残留关注（不阻塞）：challenge 标签探测不感知 Markdown 结构（代码围栏内假行可误判）、多行 summary 残留孤行（预存限制）、覆盖记录 \d{1,6} 与声明 \d+ 仍不对称（≥7 位记录被静默忽略但无死锁路径）、Plan Touched Files 未列 spec-state.js/validate.js 由 Execute Log 披露。均建议记入 Learning Record。
Challenge Executed By: subagent:challenge-v412
Challenge Executed At: 2026-08-23T05:57:19.627Z
Challenge Evidence: PASS_WITH_CONCERNS - 独立子代理审核通过：git diff 证实改动限于声明的五文件+测试文件且与 Plan/Execute Log 一致（Step 4 BUGFIX_ESCALATED 偏差描述与代码逐字吻合）；node --test tests/commands.test.js 162 pass 全绿、npm test 721 tests/716 pass/0 fail；临时 fixture 实测 executedBy 注入逐字落盘、缺标签 exit 1 不写文件、cruise 三路径（legacy 有-Spec 保账本/无-Spec discover 引导/migration-required 引导不建账本）全部符合预期；两处平行实现诊断消息一致且有去重。残留关注（不阻塞）：challenge 标签探测不感知 Markdown 结构（代码围栏内假行可误判）、多行 summary 残留孤行（预存限制）、覆盖记录 \d{1,6} 与声明 \d+ 仍不对称（≥7 位记录被静默忽略但无死锁路径）、Plan Touched Files 未列 spec-state.js/validate.js 由 Execute Log 披露。均建议记入 Learning Record。

<!-- sdd-autonomy:start -->
## Autonomy Control
Autonomy Schema: 1

### Event: evt-20260823045356670
Event Type: task_authorization
Mode: auto
Gate: 
Decision: authorized
Scope Digest: sha256:af7786ac6af2ab47c10cef4ebf159fb38fab091a64379722daea67722d622be8
Risk Snapshot: sha256:8a6cead4385ed4394247b71692fb729b0563f8e1bd4818a8c6c82940e9e099ba
Plan Digest: 
Authorized Actors: main,worker,research-reviewer,challenge-reviewer
Authorized By: human:liuying
Authorized At: 2026-08-23T04:53:56.670Z
Authorization Evidence: user approved starting task v4.12-gate-integrity-fixes (scope 1+2+3) in chat on 2026-08-23 with auto mode carried over
Invalidated At: 
Invalidation Reason: 

### Event: evt-20260823045424882
Event Type: plan_activation
Mode: auto
Gate: Plan
Decision: activated
Scope Digest: sha256:af7786ac6af2ab47c10cef4ebf159fb38fab091a64379722daea67722d622be8
Risk Snapshot: sha256:8a6cead4385ed4394247b71692fb729b0563f8e1bd4818a8c6c82940e9e099ba
Plan Digest: sha256:262bdd902f13f0254cb6f6fe578c6a5e3e65ff35338b95f7ab0fda1d0f06a3c3
Authorized Actors: main,worker,research-reviewer,challenge-reviewer
Authorized By: agent:main
Authorized At: 2026-08-23T04:54:24.882Z
Authorization Evidence: Plan approved by agent:main with gate evidence; auto mode plan_activation on 2026-08-23
Invalidated At: 
Invalidation Reason: 

<!-- sdd-autonomy:end -->

## Archive Authorization

Archive Authorized By: human:liuying
Archive Authorized At: 2026-08-23T06:41:23.846Z
Archive Authorization Evidence: current user authorized archive in chat ('同意') on 2026-08-23

---
<!-- Archive summary on 2026-08-23 -->

## 目标摘要
修复三个门禁命令的数据完整性缺陷：challenge 结果写入的替换注入、AC 编号宽度不对称导致的覆盖死锁、cruise --record-run 在 migration-required 项目上的守卫绕过。

## 最终方案
三个修复各自最小化：1) challenge.js 六个 .replace 全部改函数替换 function(m){ return label + ': ' + value; } 并在替换前校验六个标签行都存在（用 frontmatter 式 /^Label:/m 探测），缺则列出缺失标签 exit 1。2) acCoverageRecords 正则放宽为 (AC-\d{1,6}) 后按非三位判定：在 acCoverageContractIssues / validateAcCoverage 对声明 id 与记录 id 做「非三位」检查并产出 blocker「AC id must be zero-padded three digits: AC-42 -> AC-042」？——不引入自动补零转换，仅诊断，因为自动改写会篡改用户工件；同时 acceptanceFacts 声明解析保持 \d+ 但在 acceptance issues 里对非三位声明追加同一诊断。3) cruise.js 守卫条件从 nextAction === 'discover_spec' 扩为 (discover_spec || migrate_autonomy_config) 且 state.specPath 为空时同样走 CRUISE UNAVAILABLE 引导（文案按 nextAction 区分 discover/migrate 提示），--record-run 在 return 之前自然跳过。

## 关键约束
不改变有-Spec、合法输入下的任何现有行为；不引入新依赖；AC 正则以 \d{3} 为准（与现有全部测试/文档一致）。

## 坑点与风险
任何含用户文本的 .replace 一律函数形式 function(){return ...}；写入前校验目标标签行存在、缺失显式报错不写文件。同一契约的多处正则必须同宽并在一处定义。扩展条件守卫时先跑相关回归测试再继续（本次被既有测试 1973 拦住，证明回归网有效）。Plan 的 Touched Files 应直接列出平行实现的全部文件点，而不是靠「同名逻辑点」事后解释。
