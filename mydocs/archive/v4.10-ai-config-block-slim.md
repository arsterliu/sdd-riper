---
date: 2026-08-13
task-name: "ai-config-block-slim"
mode: lite
autonomy-mode: "auto"
autonomy-mode-source: "discover-override"
status: archived
reopened-from: ""
context-source: "none"
ui-impact: "no"
visual-context-intent: "not-applicable"
visual-evidence: ""
visual-evidence-file: ""
diff-base: "2d1425261a1135dcec4712446c92b116bbb3dcf5"
design-file: "mydocs/archive/v4.10-ai-config-block-slim.design.md"
execute-log-file: "mydocs/archive/v4.10-ai-config-block-slim.execute.md"
learning-file: "mydocs/archive/v4.10-ai-config-block-slim.learning.md"
project-profile-revision: ""
project-profile-digest: ""
affected-units: ""
---

<!-- Keep template headings and labels in English. Fill actual narrative content in Chinese. -->

## Summary

将 AI 配置生成块从"内联 45 条规则"重构为"单一数据源 src/core/ai-config-rules.js（19 条 canonical 规则）+ 组装器"，并新增 parity 测试强制 SKILL.md 与生成块一致；顺带改写 README/GUIDE 中的高理解成本段落，把协议细节收敛到权威文档。

## Intake
requirement: AI 配置生成块（sddBlock）过长：约 45 条规则一口气写入 AGENTS.md/CLAUDE.md/.cursorrules/copilot-instructions.md 四个目标，视觉治理、Figma、E2E 锁等细则稀释核心门禁；同时这批规则与 SKILL.md 的 Non-Negotiable Rules 双源维护（JS 字符串数组 vs Markdown 列表），内容漂移只是时间问题。
goal: sddBlock() 精简为核心门禁规则，细则以引用方式指向 REFERENCE.md/SKILL.md；规则文本收敛为单一数据源，_gen-ai-configs.js 与 SKILL.md 不再各自维护平行副本；init 幂等性与托管块刷新行为有回归测试保障。根目录现有手写前言不在本任务范围内。

## Requirement Review

需求来自两轮文档可读性分析，目标明确：(1) `sddBlock()` 生成块瘦身；(2) 规则文本单一数据源。无需求歧义；风险集中在"测试把生成块的具体字符串钉死"这一契约上——瘦身必须与测试同步演进。用户已明确排除：根目录 4 个文件标记外的手写前言清理（不在本任务范围）。

## Findings

1. 生成机制：`src/commands/_gen-ai-configs.js` 的 `sddBlock()` 是唯一生成函数，输出包在 `<!-- sdd-riper:start/end -->` 托管块内，写入 AGENTS.md / CLAUDE.md / .cursorrules / .github/copilot-instructions.md 四个目标；`upsertManagedBlock` 保证重复 init 只刷新标记内内容，幂等性已有测试（tests/commands.test.js L529-599，tests/governance-contract.test.js L555-578）。
2. 测试钉住的内容契约（瘦身硬约束，Design/AC 须携带完整清单）：
   - 生成块必须含"创建 Spec 前询问 version/task-name 与 context"规则（tests/commands.test.js 约 L2470-2480）；
   - 生成块必须含 reviewer 授权指引字符串（`subagent:<id>`、`fresh task/plan authorization`、`Plan Approval alone is insufficient` 等，约 L2506-2520），这些字符串来自 `src/core/reviewer-guidance.js`；
   - `_gen-ai-configs.js` 源码本身必须含归档授权三句（request explicit archive authorization / must not construct / audit declaration，约 L2490-2504）与 Cruise 分工句（`Cruise orchestrator`、`main agent`、`Challenge reviewer`、`read-only`，约 L2720-2737）；
   - 另钉住：生命周期行（Research -> ... -> Archive）、e2e/Provider 规则、AUTONOMY_MODE、Cruise Driver、Independent Review、Challenge Evidence 受控记录等精确串（tests/commands.test.js 约 L529-537、tests/governance-contract.test.js 约 L582-587）；以 jest 全绿为兜底。
3. 既有单一数据源模式：`src/core/reviewer-guidance.js` 导出规则文本常量，已被 challenge/next/validate/_gen-ai-configs/spec-state 五处消费——本任务应扩展此模式而非另起炉灶。
4. 消费项目约束：`sdd init` 只铺 mydocs 目录、.sdd-config 和 4 个 AI 配置；REFERENCE.md 与 protocols/ 不进入消费项目。`install-skill` 把整个包（含 protocols/、SKILL.md、GUIDE.md 等，但**不含 REFERENCE.md**）复制到宿主机 skill 目录。因此：(a) 生成块对"无 skill 环境"是唯一协议文档，必须自包含核心治理语义；(b) 生成块内的引用只能指向 skill（安装了才存在），**不得引用 REFERENCE.md**——它在消费项目和 skill 安装中均不可达。
5. 双源现状：SKILL.md 的 Non-Negotiable Rules（15 条）与 `sddBlock()` 的 Hard rules（约 30 条）内容重叠但各自手写；仓库已用"同一字符串必须在多个文件出现"的 parity 测试防止部分规则漂移。
6. 项目约定：Jest 测试（node assert 风格）、src 代码 ES5（var、函数声明）、制品内容中文、标签英文。

## Open Questions

1. 精简幅度与保留清单 → 已由用户确认（2026-08-13）：合并压缩至 ≤20 行。保留全部被测试钉住的规则与核心门禁；合并重复语义（4 条 "In every autonomy mode" → 1 条，3 条归档授权 → 1 条）；视觉/E2E/Profile/Quality 能力路由压缩为一条总原则。
2. SKILL.md 的 Non-Negotiable Rules 如何处理 → 已由用户确认（2026-08-13）：SKILL.md 保持手写全长文档；新建共享常量模块承载跨表面规则文本，扩展 parity 测试强制 SKILL.md 与生成块不漂移。
3. README 能力路由两段（L56/L58）可读性改写 → 已由用户确认（2026-08-13，scope expansion）：任务未归档，用户明确授权将 README 改写并入当前任务。合并为"场景→AI 动作"表格，删除 Provider 初始化时序与 realpath 安全检查等协议细节（属 REFERENCE 层面），重复安全约束收敛为一处。
4. 其余同类可读性问题（scope expansion 延续，2026-08-13）：用户要求"再检查有没有类似问题"，确认改写 4 处重处——README 视觉段、GUIDE 场景一（Profile/视觉术语）、场景二（Quality Plan 重复段 + realpath 细节）、场景三（Provider 锁/时序 + Figma 细节）。连带更新钉住旧协议措辞的测试：tests/visual-evidence-guidance.test.js（file list 移除 README/GUIDE，视觉治理语义由 SKILL.md / ai-config-rules.js / INTEGRATIONS.md 承载）、tests/quality-cli.test.js 与 tests/verification-cli.test.js（断言改为用户视角本质语义，移除 INIT_LOCKED 必须出现在 guide 的要求）。中度问题（README L42/45/51、GUIDE L117/121）不改。

## Assumptions

1. 不清理根目录 4 个 AI 配置文件标记外的手写前言（用户明确排除）。
2. 不新增 CLI 命令（如 `sdd rules`）作为规则分发渠道，控制范围。
3. 生成块面向"可能未安装 skill 的消费项目 agent"，核心门禁语义必须自包含；瘦身靠合并重复语义与压缩措辞，不靠删除治理行为。
4. 已生成项目的旧托管块不受本任务影响，下次 `sdd init` 时才刷新，无需迁移。

Research Reviewed By: subagent:af0d003aa93a08812
Research Reviewed At: 2026-08-13T08:28:57Z

## Confirmed Requirement
<!-- Fill each label with analysis in Chinese. These five elements are the structured output of Research. -->
Scope Boundary: 仅改 `src/commands/_gen-ai-configs.js`、（如采用）新增 `src/core/` 共享规则常量模块、SKILL.md 的 Non-Negotiable Rules 同步策略、tests/ 中相关契约测试。不清理根目录手写前言，不动 REFERENCE.md 结构，不改运行时门禁逻辑。
Irreversibility: 完全可逆——源码/文档/测试改动，git 可回滚；已生成项目的旧托管块不受破坏。
Impact Radius: 影响所有未来 `sdd init` 产出项目的 agent 指令内容（AGENTS.md/CLAUDE.md/.cursorrules/copilot-instructions.md 四处托管块）；对本仓库自身的测试契约有多处联动。
Dependencies & Constraints: 受 tests/commands.test.js 与 tests/governance-contract.test.js 钉住的字符串契约约束（可同步更新测试，但治理语义不得丢失）；遵循 `src/core/reviewer-guidance.js` 共享常量模式；src 保持 ES5 风格；jest 全绿。
Acceptance Intent: (a) 生成块 Hard rules 段压缩至 ≤20 行（以用户确认口径为准）且核心治理语义无丢失；(b) 跨表面规则文本有单一来源（共享常量模块），SKILL.md 与生成器的一致性由 parity 测试强制；已知残余风险：parity 基于子串存在性，未钉措辞仍可能漂移，本任务接受；(c) init 幂等/托管块刷新行为有回归测试；(d) 全部测试通过。

## Innovate Options

Innovate: Skipped, Reason: 方案空间已由用户两项决策（合并压缩至 ≤20 行、共享常量模块 + parity 测试）+ 测试钉住契约收窄；剩余取舍（行数口径、附录结构、reviewer-guidance 字符串路径）已在 Design Note 的 Approach/Risks 中记录，无需多方案比较。

## Design Reference
<!-- Design Note is an independent artifact in design-file. -->
design-file: "mydocs/archive/v4.10-ai-config-block-slim.design.md"

## Acceptance Criteria
<!-- Use AC-### items with English metadata labels; write scenario text in Chinese.
Provider: <required for e2e; named provider id>
-->

AC-001: 生成块 Hard rules 段压缩至 ≤20 行（仅计 "Hard rules:" 下列出的规则行）。
Requirement: 行数上限
Type: constraint
Verification: unit
Test: tests/ai-config-parity.test.js
Method: tdd

AC-002: 新增 src/core/ai-config-rules.js 作为规则文本单一数据源；src/commands/_gen-ai-configs.js 从它消费，不再内联维护平行规则副本（reviewer-guidance 字符串可继续经模块注入）。
Requirement: 单一数据源
Type: architecture
Verification: unit
Test: tests/ai-config-parity.test.js
Method: tdd

AC-003: 所有被现有测试钉住的字符串在生成块（AGENTS.md/CLAUDE.md/.cursorrules/copilot-instructions.md）中保留；tests/commands.test.js、tests/governance-contract.test.js、tests/autonomy-guidance.test.js、tests/visual-evidence-guidance.test.js 定向全绿。
Requirement: 契约不回归
Type: constraint
Verification: integration
Test: tests/commands.test.js tests/governance-contract.test.js tests/autonomy-guidance.test.js tests/visual-evidence-guidance.test.js
Method: tdd

AC-004: 新增 parity 测试：CORE_RULES 每条及关键钉住短语必须出现在 SKILL.md，防止 SKILL.md 与生成块漂移。
Requirement: 防漂移保障
Type: constraint
Verification: unit
Test: tests/ai-config-parity.test.js
Method: tdd

AC-005: init 幂等与托管块刷新行为回归测试保持通过（第二次 init 字节幂等、单块标记、保留标记外项目文本）。
Requirement: 行为不回归
Type: constraint
Verification: integration
Test: tests/governance-contract.test.js
Method: tdd

AC-006: 针对 src/commands/_gen-ai-configs.js 源码做内容断言的测试 file list 更新为同时覆盖 src/core/ai-config-rules.js。
Requirement: 断言对象迁移
Type: constraint
Verification: unit
Test: tests/commands.test.js tests/autonomy-guidance.test.js tests/visual-evidence-guidance.test.js
Method: tdd

AC-007: 生成块仍自包含核心门禁，内容中不引用 REFERENCE.md（已确认其在消费项目与 skill 安装中均不可达）。
Requirement: 自包含约束
Type: constraint
Verification: unit
Test: tests/ai-config-parity.test.js
Method: tdd

AC-008: 全量 jest 通过。
Requirement: 全量回归
Type: constraint
Verification: integration
Test: npx jest
Method: tdd

## Plan

Step 1: 新建 src/core/ai-config-rules.js —— 导出 CORE_RULES（16 条，满足全部钉住正则）、CAPABILITY_ROUTING（视觉/Profile/Quality 细则）、ENTRY_POINTS、PROJECT_CONFIG、suffixes（Claude/Cursor/Copilot 提示）。
- Files: src/core/ai-config-rules.js
- AC: AC-001, AC-002, AC-003, AC-007
- Verification: node -e "require('./src/core/ai-config-rules.js')" 无异常；随后 Step 3/4 测试覆盖

Step 2: 改造 src/commands/_gen-ai-configs.js —— sddBlock() 改为从 ai-config-rules.js 组装 CORE_RULES + CAPABILITY_ROUTING + ENTRY_POINTS + PROJECT_CONFIG + 目标后缀；upsertManagedBlock/run() 逻辑不动。
- Files: src/commands/_gen-ai-configs.js
- AC: AC-002, AC-003
- Verification: npx jest tests/commands.test.js tests/governance-contract.test.js

Step 3: 更新 3 个测试文件的 file list —— 针对 src/commands/_gen-ai-configs.js 源码做内容断言的处，加入 src/core/ai-config-rules.js。
- Files: tests/commands.test.js tests/autonomy-guidance.test.js tests/visual-evidence-guidance.test.js
- AC: AC-006, AC-003
- Verification: npx jest tests/commands.test.js tests/autonomy-guidance.test.js tests/visual-evidence-guidance.test.js

Step 4: 新增 tests/ai-config-parity.test.js —— 断言 CORE_RULES 每条及关键钉住短语出现在 SKILL.md；Hard rules 段行数 ≤20；生成块内容不引用 REFERENCE.md。
- Files: tests/ai-config-parity.test.js
- AC: AC-001, AC-004, AC-007
- Verification: npx jest tests/ai-config-parity.test.js

Step 5: 对齐 SKILL.md Non-Negotiable Rules 措辞使其包含每条 CORE_RULES（如现有措辞不匹配，做最小改写）。
- Files: SKILL.md
- AC: AC-004
- Verification: npx jest tests/ai-config-parity.test.js tests/autonomy-guidance.test.js tests/visual-evidence-guidance.test.js

Step 6: 手动 smoke —— 临时目录跑 sdd init --mode lite，检查 4 个生成文件内容、块唯一、- Mode: lite。
- Files: （临时目录）
- AC: AC-003, AC-005
- Verification: bash + Read 抽查生成文件

Step 7: completion-verification —— 四轴自查 + AC Coverage 汇总。
- Files: mydocs/logs/v4.10-ai-config-block-slim.execute.md
- AC: AC-008
- Verification: npx jest（全量）

Plan Approved By: agent:claude-code-main
Approved At: 2026-08-13T08:35:00Z
Gate Evidence: 基线定向测试 221/221 通过（node --test tests/commands.test.js tests/governance-contract.test.js tests/autonomy-guidance.test.js tests/visual-evidence-guidance.test.js）；Plan 步骤均含文件边界、具体改动、AC 映射与验证命令；Design Note 已逐条核对测试钉住字符串契约。

## Execute Log Reference
execute-log-file: "mydocs/archive/v4.10-ai-config-block-slim.execute.md"

## Completion Verification
<!-- AC Coverage records belong to a formal execution Step's `AC Coverage:` section. The `completion-verification` records only the four-axis self-check, result, and verification; Summary is not evidence. This replaces the former Review Summary section. -->

Challenge Verdict: PASS
Backtrack Target: Ready
Challenge Summary: 最终复审：六轴通过；Step 9/10 文档可读性改写（README/GUIDE）与测试联动（visual-evidence-guidance/quality-cli/verification-cli）经独立 reviewer 复核，删除的协议细节均在 REFERENCE.md/SKILL.md/ai-config-rules.js 权威来源完整保留；全量 586 测试 0 失败。
Challenge Executed By: subagent:a05a48b7bf8019810
Challenge Executed At: 2026-08-13T10:45:21.955Z
Challenge Evidence: PASS - 最终复审：六轴通过；Step 9/10 文档可读性改写（README/GUIDE）与测试联动（visual-evidence-guidance/quality-cli/verification-cli）经独立 reviewer 复核，删除的协议细节均在 REFERENCE.md/SKILL.md/ai-config-rules.js 权威来源完整保留；全量 586 测试 0 失败。

<!-- sdd-autonomy:start -->
## Autonomy Control
Autonomy Schema: 1

### Event: evt-20260813100640042
Event Type: task_authorization
Mode: auto
Gate: 
Decision: authorized
Scope Digest: sha256:41878a13c65c4779a02f97c61ff192315700c9dab9638166d43eeae9e28aee06
Risk Snapshot: sha256:5d2d3ceb7abe552344276d47d36a8175b7aeb250a9bf0bf00e850cd23ecf2e43
Plan Digest: 
Authorized Actors: main,worker,research-reviewer,challenge-reviewer
Authorized By: human:liuying
Authorized At: 2026-08-13T10:06:40.042Z
Authorization Evidence: 用户在 discover 时选择 auto 协作方式，对当前 scope digest 授权在确认范围与风险内持续推进
Invalidated At: 
Invalidation Reason: 

### Event: evt-20260813100651496
Event Type: plan_activation
Mode: auto
Gate: Plan
Decision: activated
Scope Digest: sha256:41878a13c65c4779a02f97c61ff192315700c9dab9638166d43eeae9e28aee06
Risk Snapshot: sha256:5d2d3ceb7abe552344276d47d36a8175b7aeb250a9bf0bf00e850cd23ecf2e43
Plan Digest: sha256:b1c18f9924c801d79f843a743d5ce32f958f9c4a82e803c3979bd67cae39a29e
Authorized Actors: main,worker,research-reviewer,challenge-reviewer
Authorized By: agent:claude-code-main
Authorized At: 2026-08-13T10:06:51.496Z
Authorization Evidence: Plan 已批准（Gate Evidence：基线 221 定向测试通过），auto 模式激活计划执行
Invalidated At: 
Invalidation Reason: 

<!-- sdd-autonomy:end -->

## Archive Authorization

Archive Authorized By: human:liuying
Archive Authorized At: 2026-08-13T11:00:31.251Z
Archive Authorization Evidence: 用户确认任务结果符合目标，对本次归档明确授权（2026-08-13）

---
<!-- Archive summary on 2026-08-13 -->

## 目标摘要
将 AI 配置生成块从"内联 45 条规则"重构为"单一数据源 src/core/ai-config-rules.js（19 条 canonical 规则）+ 组装器"，并新增 parity 测试强制 SKILL.md 与生成块一致；顺带改写 README/GUIDE 中的高理解成本段落，把协议细节收敛到权威文档。

## 最终方案
新建 src/core/ai-config-rules.js 作为生成块规则文本的单一数据源（19 条 canonical 规则 + 能力路由附录），_gen-ai-configs.js 改为消费组装；SKILL.md 保持手写，由新增 parity 测试强制与生成块一致。相比"内联 45 条规则"或"生成 SKILL.md"：消除双源漂移且不引入构建步骤。

## 关键约束
受 tests/commands.test.js 与 tests/governance-contract.test.js 钉住的字符串契约约束（可同步更新测试，但治理语义不得丢失）；遵循 `src/core/reviewer-guidance.js` 共享常量模式；src 保持 ES5 风格；jest 全绿。

## 坑点与风险
契约测试应断言"生成产物（生成后的 block）或规则数据源模块"的内容，而不是断言"组装器源码"——组装器只负责拼装，不拥有规则文本。凡规则文本存在单一数据源时，内容断言一律指向该数据源模块。
