---
date: 2026-08-09
task-name: "documentation-workflow-consistency"
mode: micro
status: archived
reopened-from: ""
context-source: "README.md; GUIDE.md; SKILL.md; protocols/sdd-riper-one.md; protocols/sdd-riper-one-light.md; templates/archive.md; lib/common.js"
ui-impact: "no"
visual-context-intent: "not-applicable"
visual-evidence: ""
visual-evidence-file: ""
diff-base: "054328235a8bd730edbe8500ee1acabc3dd6931b"
design-file: ""
execute-log-file: "mydocs/archive/v4.7-documentation-workflow-consistency.execute.md"
learning-file: "mydocs/archive/v4.7-documentation-workflow-consistency.learning.md"
project-profile-revision: ""
project-profile-digest: ""
affected-units: ""
---

<!-- Keep template headings and labels in English. Fill actual narrative content in Chinese. -->

## Summary

校正已核实的指南、Skill、快捷协议与归档模板表述，将 README 重构为读者入口页，并把 GUIDE 改造成面向使用场景的人话指南；现有精确字段、门禁、安全边界和内部调度说明迁入独立 REFERENCE。全部变更仅涉及文档、模板及其分层回归，不改变产品、CLI、Schema 或历史制品行为。

## Intake
requirement: 修正已核实的用户指南、agent Skill、快捷协议和归档模板，使其与当前实现一致，并让 README / GUIDE 能被普通使用者顺畅理解。
goal: 消除工作流语义误导，建立 README 快速入口、GUIDE 场景指南、REFERENCE 精确协议的三层文档结构，不改任何产品或 CLI 行为。
constraints: 仅修改文档与模板，并可调整验证文档分层的既有 `tests/commands.test.js` 回归；GUIDE 使用人话解释实际任务旅程，REFERENCE 完整保留字段、门禁、安全和调度契约；保持中文说明/英文标签约定；不改 src、bin、schema、生产行为或历史归档；执行和归档仍须遵守现有门禁。
<!-- Micro skips Research, Innovate, and standalone Design. -->

## Plan
Impact Scope: 仅文档与模板：README、GUIDE、新增 REFERENCE、仓库内 Skill、两个快捷协议和归档模板；并依当前用户授权将 `.sdd-config` 的 Cruise 开关设为启用。README 作为快速入口，GUIDE 作为场景化人话指南，REFERENCE / SKILL / protocols 承载精确协议；同步更新仅检查文档分层的现有回归。不改生产代码、CLI、Schema、生产配置或历史归档。
Data Impact: 无；不迁移、不生成、不删除任何业务或历史数据。
Interface Impact: 无行为或公开接口变更；仅使使用者看到的工作流说明与现有实现一致。
Selected Option: 采用“快速入口 + 场景指南 + 精确参考”的三层渐进式披露：README 帮助新读者判断用途并开始首个任务；GUIDE 以开始任务、方案确认、执行、失败修复、完成归档和按需高级能力等场景说明用户与 AI 各自要做什么；REFERENCE 完整承接字段、门禁矩阵、安全边界、调度模型和命令细节。
Acceptance:
- Completion Verification 仅说明四轴自查、Result、Verification 与时间戳；AC Coverage 明确归属前序正式 Execute Step，不再表述为 Completion Verification 的汇总字段。
- Challenge 与 Cruise 的说明准确区分“由工作流/`next` 路由”与“实际执行仍须满足授权和阶段门禁”，不再暗示自动启动审查代理或修复循环。
- 归档模板使用 Challenge 结论标题，且不留下已废弃的 Review 结论表述。
- 微型 Spec 模板、Skill、指南和轻量协议均要求 `Selected Option`，使无独立 Design 的任务仍可生成归档摘要。
- README 的开头用自然语言说明产品用途、适用对象和最短上手路径；不要求读者先理解 Spec、Gate、Cruise、Provider 或审计字段。
- README 的“三分钟开始”优先给出 AI 引导路径：完成一次安装与 Skill 注册后，用户只需向 AI 说明版本、任务名、目标和参考资料；CLI 路径作为直接操作的备选。
- README 保留安装、Skill 注册、日常更新、按意图分组的常用命令、目录概览和高级能力入口；不重复 REFERENCE / SKILL 的协议规则、逐阶段字段或安全边界细节。
- GUIDE 不以英文授权规则、harness、控制面或门禁字段开场；首先说明适用对象和任务旅程，并按“什么时候用、你提供什么、AI 做什么、你确认什么、完成标志”组织主要场景。
- GUIDE 保留普通使用者需要理解的安全停点和命令示例，但不展开字段枚举、状态矩阵、调度职责、路径/realpath、防伪或 Provider Schema 等协议实现细节。
- REFERENCE 完整保留原 GUIDE 中仍正确的精确协议内容，并由 GUIDE 提供清晰链接；迁移不得弱化归档授权、审查授权、Challenge 防伪、AC Coverage、Provider fail-closed、历史制品可读或路径安全边界。
- Profile、Quality、Verification、Visual 在 README 保留“什么时候需要”的简述，在 GUIDE 提供场景说明，在 REFERENCE / SKILL 保留精确工作流。
- 文档回归验证完整 agent 协议留在 REFERENCE / SKILL / 协议文件，同时验证 README 和 GUIDE 的人类入口契约；不再要求 README 或 GUIDE 逐字重复内部规则。
- REFERENCE 被加入 npm 发布文件清单，README 与 TEAM-GUIDE 中指向原 GUIDE 协议章节的链接同步改到正确层级，避免安装包缺失或悬空导航。
- REFERENCE 在不改写精确协议块的前提下增加使用说明、快速索引、常见术语和主要章节的人话摘要；读者可以先理解“这一章解决什么”，再进入字段与规则。
- REFERENCE 与 TEAM-GUIDE 的归档授权正文使用完整中文说明，并保留命令参数与状态字段；供 Agent 直接加载的英文硬契约继续由 SKILL、protocols 和生成配置的既有断言保护，不要求人类文档复制英文整段。
- 目标文件的说明彼此一致；除当前任务 Cruise 开关和文档分层回归外，不改任何实现文件、生产测试、生产配置或历史归档。
Verification:
- 静态核对目标文档，确认不存在 `AC Coverage Summary`、将 Challenge/Cruise 表述为自动进入或已废弃 `Review 结论` 标题的残留。
- 运行既有工作流门禁事实回归，确认文档所描述的 Completion Verification / AC Coverage 边界仍由当前实现覆盖。
- 运行文档差异空白检查，并核对变更范围仅限目标文档、文档分层回归、当前任务 Cruise 开关、当前 Spec、Execute Log 与 Cruise 运行账本。
- 从“首次接触”“正在执行任务”“需要高级能力”三条读者路径人工复读 README，确认每条路径都能在不先读 GUIDE / SKILL 的情况下找到下一步；并核对命令、链接和行为表述未偏离 CLI。
- 运行 README 入口回归，确认 AI 引导路径位于直接 CLI 路径之前，且列出版本、任务名、目标与参考资料四项输入。
- 人工复读 GUIDE 的“首次任务、任务卡住、准备归档、启用高级能力”四条路径，确认每条路径无需先理解协议字段即可找到用户动作、AI 动作、确认点和完成标志。
- 运行 GUIDE 分层回归：GUIDE 包含场景入口和 REFERENCE 链接，不以协议授权英文开场；REFERENCE、SKILL 和 protocols 继续包含精确授权、审查、Cruise 与 Coverage 契约。
- 核对迁移前 GUIDE 的标题与关键协议标记均能在 GUIDE 或 REFERENCE 中找到，防止拆分时丢失有效内容；运行链接/doctor 检查和 `git diff --check`。
- 运行 REFERENCE 可读性回归，确认使用说明与快速索引位于协议正文之前、术语表覆盖核心概念、主要章节均有人话摘要，同时精确归档授权、审查身份、Coverage、Cruise、realpath 和 Provider 标记保持存在。
- 运行授权分层回归，确认 REFERENCE 与 TEAM-GUIDE 不再包含 `Archive authorization rule:` 英文段落，但中文正文仍明确当前用户一次性授权、不得自行构造参数和审计声明非身份认证；Agent 协议文件继续保留英文硬契约。
Scope: 统一上述已确认的文档契约；按当前用户授权启用 Cruise；将 README 重构为读者优先入口，并建立 README / GUIDE / REFERENCE 三层结构。
Touched Files: .sdd-config; README.md; GUIDE.md; REFERENCE.md; TEAM-GUIDE.md; package.json; SKILL.md; protocols/sdd-riper-one.md; protocols/sdd-riper-one-light.md; templates/archive.md; templates/spec-micro.md; tests/commands.test.js; mydocs/specs/v4.7-documentation-workflow-consistency.md; mydocs/logs/v4.7-documentation-workflow-consistency.execute.md; mydocs/learnings/v4.7-documentation-workflow-consistency.learning.md; mydocs/runs/v4.7-documentation-workflow-consistency.cruise.jsonl。
Change:
1. 按当前用户“自动推进至归档”的授权启用当前项目 Cruise 开关，不改迭代预算或其他配置。
2. 以当前解析规则为准，修正 Completion Verification 及其示例，保留正式 Execute Step 的 `AC Coverage:` 记录位置。
3. 将 Challenge/Cruise 的“自动进入”措辞改为工作流路由说明，并明确标准/轻量任务如需调度审查代理仍要获得当前用户授权。
4. 将归档模板的遗留 Review 标题改为 Challenge 标题。
5. 将 `Selected Option` 加入微型 Spec 的 Plan 模板和对应指南，补齐归档摘要所需的决策记录；当前 Spec 写入该字段后再归档。
6. 重写 README 顶部为“它解决什么问题 / 适合谁 / 三分钟开始 / 下一步去哪”的入口路径；把归档授权等 agent 安全约束从首屏移至 REFERENCE / SKILL 的详细说明链接。
7. 将 README 的流程说明压缩为人类可读的任务旅程；保留一行阶段图和“失败会回到对应阶段”的说明，不展开字段清单、审查身份或 Cruise 驱动细节。
8. 将常用命令改按“开始项目、创建和推进任务、检查和归档、可选高级能力”分组；保留准确命令语法，删除首次使用不需要的实现解释。
9. 将 Profile、Quality、Verification、Visual 改为四张简短的“什么时候使用”卡片并链接 GUIDE；保留目录结构与文档导航，删除 README 中的深度协议段落。
10. 更新文档回归的受众边界：完整 agent 协议断言不再要求 README 逐字重复，改为验证 README 的上手入口和 GUIDE 链接。
11. 在“三分钟开始”中新增优先的 AI 引导路径和可复制的任务说明模板；将现有 CLI 初始化/创建/推进命令明确降为直接操作的备选路径，并扩展入口回归。
12. 新增 REFERENCE，将 GUIDE 中仍正确的精确协议、字段、矩阵、安全边界、调度模型和命令参考迁入；迁移以保留内容为主，不重新解释生产行为。
13. 重写 GUIDE 为场景化人话指南：先解释使用者会经历什么，再按统一五段结构覆盖开始任务、确认方案、执行与验证、失败修复、归档和高级能力；每个场景链接到 REFERENCE 的对应精确规则。
14. 更新 README / GUIDE / REFERENCE / TEAM-GUIDE 的相互导航、npm 发布文件清单和文档回归：先写 GUIDE 入口与协议保留的失败断言，再完成拆分并跑定向、命令级、门禁事实、doctor 与空白检查。
15. 优化 REFERENCE 的查阅体验：先新增“可读导航但不弱化协议”的失败断言，再把开头改为用途说明与快速索引，增加核心术语表，并在主要章节开头加入一两句“本章回答”；原有精确规则只移动或保留，不摘要替换。
16. 将 REFERENCE 与 TEAM-GUIDE 授权章节的整段英文改为语义等价的中文正文，保留状态值、身份格式和 CLI 参数；把英文逐字断言限制在 Agent 权威协议文件，并用红绿回归同时保护中文可读性与英文 Agent 契约。
Blast Radius: 文档改动仅影响后续使用者理解和新任务模板字段；README 会保持简短，GUIDE 会显著缩短并转为普通使用者场景，REFERENCE / SKILL / protocols 继续承载完整协议。文档回归改变 README 与 GUIDE 的受众契约，不影响 CLI 行为。微型任务会多记录一项已选择方案，供现有归档摘要读取。Cruise 开关影响当前项目后续任务是否可记录自主巡航运行账本。不影响已有制品读取、命令输出、验证 Provider 或运行时逻辑。

Plan Approved By: human:liuy
Approved At: 2026-08-11T18:17:13+08:00
Gate Evidence: 当前用户先确认采用 README / GUIDE / REFERENCE 三层设计，随后在收到更新后 Plan 的审阅请求后回复“ok”，明确允许按该 Plan 继续；REFERENCE 必须随 npm 包发布并修正关联导航，属于实现该已批准拆分所必需的窄范围配套。完成三层拆分后，当前用户又明确同意为 REFERENCE 增加使用说明、目录、术语解释与章节摘要。当前用户随后选中归档授权英文段落询问原因，并在收到“REFERENCE 改为中文、英文硬契约留在 SKILL / protocols”的明确方案后回复“ok”，授权 Change 16。

## Execute Log Reference
execute-log-file: "mydocs/archive/v4.7-documentation-workflow-consistency.execute.md"

## Completion Verification
<!-- AC Coverage records belong to a formal execution Step's `AC Coverage:` section. The `completion-verification` records only the four-axis self-check, result, and verification; Summary is not evidence. This replaces the former Review Summary section. -->

Challenge Verdict: PASS
Backtrack Target: Ready
Challenge Summary: 内联对抗复审确认：归档授权的语言分层现在与文档受众一致。REFERENCE 和 TEAM-GUIDE 使用中文完整表达当前用户一次性授权、禁止 Agent 自行构造或从 Ready/PASS/Plan Approval/Challenge/旧授权推断许可、human:<name> 仅为审计声明且非身份认证；REFERENCE 同时保留 NEXT_ACTION、身份格式和完整 CLI 参数。SKILL、两个 protocols 与生成配置继续保留英文硬契约，并由独立回归逐字保护。两轮定向测试均先红后绿；最终 commands 149/149、workflow-gate-facts 18/18、doctor、中英文分层静态检查与 git diff --check 全部通过。未修改 CLI、Schema、生产逻辑或历史制品，用户原有 .gitignore 与 Playwright fixture 变更未触碰。
Challenge Executed By: inline
Challenge Executed At: 2026-08-11T11:19:31.168Z
Challenge Evidence: PASS - 内联对抗复审确认：归档授权的语言分层现在与文档受众一致。REFERENCE 和 TEAM-GUIDE 使用中文完整表达当前用户一次性授权、禁止 Agent 自行构造或从 Ready/PASS/Plan Approval/Challenge/旧授权推断许可、human:<name> 仅为审计声明且非身份认证；REFERENCE 同时保留 NEXT_ACTION、身份格式和完整 CLI 参数。SKILL、两个 protocols 与生成配置继续保留英文硬契约，并由独立回归逐字保护。两轮定向测试均先红后绿；最终 commands 149/149、workflow-gate-facts 18/18、doctor、中英文分层静态检查与 git diff --check 全部通过。未修改 CLI、Schema、生产逻辑或历史制品，用户原有 .gitignore 与 Playwright fixture 变更未触碰。

## Archive Authorization

Archive Authorized By: human:liuy
Archive Authorized At: 2026-08-11T11:44:35.204Z
Archive Authorization Evidence: 好，先把现在的文档治理归档吧

---
<!-- Archive summary on 2026-08-11 -->

## 目标摘要
校正已核实的指南、Skill、快捷协议与归档模板表述，将 README 重构为读者入口页，并把 GUIDE 改造成面向使用场景的人话指南；现有精确字段、门禁、安全边界和内部调度说明迁入独立 REFERENCE。全部变更仅涉及文档、模板及其分层回归，不改变产品、CLI、Schema 或历史制品行为。

## 最终方案
采用“快速入口 + 场景指南 + 精确参考”的三层渐进式披露：README 帮助新读者判断用途并开始首个任务；GUIDE 以开始任务、方案确认、执行、失败修复、完成归档和按需高级能力等场景说明用户与 AI 各自要做什么；REFERENCE 完整承接字段、门禁矩阵、安全边界、调度模型和命令细节。

## 关键约束
仅修改文档与模板，并可调整验证文档分层的既有 `tests/commands.test.js` 回归；GUIDE 使用人话解释实际任务旅程，REFERENCE 完整保留字段、门禁、安全和调度契约；保持中文说明/英文标签约定；不改 src、bin、schema、生产行为或历史归档；执行和归档仍须遵守现有门禁。

## 坑点与风险
1. Plan 批准后收到会改变受控文件的明确用户指令时，先判断它是否持久化或影响后续任务；若是，必须在执行前或紧随执行后补充 Plan 的 Impact Scope、Touched Files、Change、Blast Radius 与相应验收/验证边界，并在 Execute Log 追加新 Step 和新的最后 Completion Verification。用户的明确追加授权可以作为该窄范围勘误的 Gate Evidence，但不能被静默当作原 Plan 已包含。
