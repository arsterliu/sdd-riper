# SDD-RIPER 使用指南

这份指南写给真正要完成任务的人。你不需要先学会一套术语，也不需要记住所有命令；只要知道自己现在处在哪个场景、需要提供什么，以及下一步由谁来做。

如果你还没有安装，请先看 [README 的“三分钟开始”](./README.md#三分钟开始)。如果你要查字段、门禁矩阵、安全边界或完整命令，请看 [REFERENCE.md](./REFERENCE.md)。

## 先看你现在要做什么

| 你现在的情况 | 从哪里开始 |
| :--- | :--- |
| 第一次使用，想让 AI 带着做 | [场景一：开始一个新任务](#场景一开始一个新任务) |
| AI 已给出方案，等你确认 | [场景二：确认方案和计划](#场景二确认方案和计划) |
| 已经确认，可以开始修改 | [场景三：执行和验证](#场景三执行和验证) |
| 测试失败、检查不过或任务跑偏 | [场景四：任务卡住或检查失败](#场景四任务卡住或检查失败) |
| 工作完成，准备收尾 | [场景五：完成与归档](#场景五完成与归档) |
| 遇到复杂工程、端到端或视觉验收 | [按需使用的高级能力](#按需使用的高级能力) |

整个过程可以先理解成一句话：

> 先把任务说清楚，再确认方案；执行时留下证据，完成后复查，最后由你决定是否归档。

通常你只需要和 AI 对话。命令行是底层控制入口，适合你想亲自检查状态或直接操作时使用。

## 先决定要多少人工参与

这是“AI 什么时候必须停下来问你”，和 standard/lite/micro 的任务大小不是一回事。

| 协作方式 | 用人话说 | 适合场景 |
| :--- | :--- | :--- |
| `auto` | 你先确认本次任务的范围并授权；AI 可自动启动计划内主 Agent、工作子 Agent 和只读审查 Agent，一路推进到归档前。 | 边界清楚、希望少打断的任务 |
| `supervised` | AI 先完成研究和 Plan；你确认 Plan，并明确授权后续自动推进，然后 AI 继续到归档前。 | 默认；既想看方案又不想逐步审批 |
| `human` | 你在关键治理节点逐次确认；AI 仍可自动做普通修改、测试和计划内调试。 | 核心、高风险或教学式协作 |

创建 Spec 前，AI 会先主动询问你选择 `auto`、`supervised` 还是 `human`，并根据任务情况推荐，通常是 `supervised`。项目默认值只是推荐，不能静默替你选择。如果你已经明确指定模式，AI 会复述并请你确认，不再重复询问。

项目设置只是新任务的默认值。每个 Spec 都会固定自己的协作方式，所以后来修改项目默认值不会悄悄改变正在进行的任务。若某个 Spec 需要临时切换，直接告诉 AI“把当前任务切换为 auto/supervised/human”；它会展示影响、使旧授权失效，并请你确认新的范围。已归档任务不会被改写。

`auto` 不是无限授权：范围扩大、新风险、重大偏差、不可逆动作、平台权限、工程画像确认、跳过 E2E 和最终归档仍会停下。`supervised` 的 Plan Approval 也不会自动等于后续自动驾驶授权。

AI 在写自治授权前只允许操作当前活动 Spec，并在项目锁内重新核对你看过的摘要。`supervised` 会同时核对 Scope digest 与 Plan digest；`auto` 在 Plan 成形后还要写一条 `plan_activation`，证明 Plan 没超出已授权的 Scope/Risk。Plan 或风险变化后旧激活自动失效，AI 会解释变化并重新请求所需确认。

如果 Verification 初始化提示 `INIT_LOCKED`，表示当前项目已有一次初始化事务在进行；它只使用一把项目锁。先检查并结束原事务，再重试，不要绕过锁。

## 场景一：开始一个新任务

### 什么时候用

你准备做一个新功能、修复一个问题、重构一块代码，或者调整文档与配置。

### 你要提供什么

至少告诉 AI 四件事：

- 版本：这次任务属于哪个交付版本，例如 `v1.0`。
- 任务名：一个短名字，例如 `login-page`。
- 目标：希望最终解决什么问题。
- 参考资料：PRD、截图、设计稿、说明文件或链接；没有就明确说“无”。

可以直接复制下面这段：

```text
请用 SDD-RIPER 帮我开始一个任务：
- 版本：v1.0
- 任务名：login-page
- 目标：完成登录页
- 参考资料：无
- 协作方式：supervised
```

### AI 会做什么

AI 会先确认这些信息，而不是替你猜版本、任务名或协作方式。然后它会创建本次任务的说明文件，把目标、范围和后续记录放在同一条任务链上。

任务大小会影响过程轻重：很小的改动可以少走几步；跨模块、高风险或不可逆的任务会要求更完整的研究和设计。

### 你需要确认什么

确认任务名、版本、目标和参考资料没有写错。如果 AI 发现范围含糊，它会继续问最少量的问题。

### 完成标志

AI 能清楚说明“这次做什么、不做什么、下一步是什么”。你也可以直接检查：

```text
sdd next <项目目录>
```

精确的任务创建规则见 [REFERENCE：RIPER 流程](./REFERENCE.md#三riper-流程)。

## 场景二：确认方案和计划

### 什么时候用

任务已经创建，AI 正在梳理现状、比较方案或准备执行计划。

### 你要提供什么

回答真正影响结果的问题，例如：

- 哪些内容明确不在本次范围内？
- 是否允许改变公开接口、数据格式或配置？
- 有没有不可接受的风险或必须保留的行为？
- 怎样才算完成？

不需要替 AI 填内部字段。你只要用业务和工程语言回答即可。

### AI 会做什么

AI 会根据任务大小完成必要的研究、方案比较和设计，再把执行步骤、影响范围和验证方法整理出来。小任务的方案会直接写进计划；较大的任务会有独立设计文件。

如果流程需要独立的自动化审查角色，AI 会先向当前用户请求授权，不会把“需要审查”直接当成“可以自行启动审查工具”。

### 你需要确认什么

重点看四件事：

- 方案确实解决了你的目标。
- 没有顺手扩大范围。
- 风险、兼容性和回退方式说得通。
- 验证方法真的能证明任务完成。

是否必须由人批准计划取决于项目设置；即使允许 AI 批准，也必须留下可检查的批准依据。

### 完成标志

计划已经批准，AI 能说明将修改哪些文件、如何验证以及发现问题时回到哪里。

精确的模式、设计和审批规则见 [REFERENCE：三种模式与审批策略](./REFERENCE.md#四三种模式与审批策略)。

## 场景三：执行和验证

### 什么时候用

方案和计划已经准备好，可以开始修改代码或文档。

### 你要提供什么

通常不需要重复描述任务。只有遇到真实取舍、需要人工判断的结果，或环境确实无法完成某项验证时，AI 才会再询问你。

### AI 会做什么

AI 会按计划逐步修改，并把实际做了什么、运行了什么检查、结果如何追加到执行记录中。遇到失败时，先定位根因，再决定修复或回到前面的阶段，不会用反复重跑掩盖问题。

验证强度取决于任务：

- 逻辑改动通常需要自动化测试。
- 接口或数据流需要集成验证。
- 关键用户路径可以使用端到端验证。
- 无法自动化的内容必须说明人工检查证据。

### 你需要确认什么

如果出现计划外改动、跳过验证、人工验收或不可逆操作，确认理由和影响是否可以接受。普通的计划内修改无需逐行批准。

### 完成标志

计划步骤都有结果，相关验证已经运行，实际改动和记录一致，并完成一次最终自查。

你可以查看当前状态：

```text
sdd resume <项目目录>
sdd validate <项目目录>
```

精确的执行记录和覆盖证据格式见 [REFERENCE：Execute](./REFERENCE.md#execute)。

## 场景四：任务卡住或检查失败

### 什么时候用

测试失败、验证证据不足、方案与目标不符、检查发现风险，或者对抗复审认为任务不能继续。

### 你要提供什么

如果失败来自你的环境或业务判断，补充缺失信息即可。不要为了尽快通过而同意伪造测试结果、截图、审批或审查证据。

### AI 会做什么

AI 会先判断问题属于哪一类：需求不清、方案有误、计划遗漏、实现错误、记录不完整，还是需要沉淀经验。然后回到对应阶段修复，而不是从头重做整个任务。

Challenge 可以理解为任务完成后的“挑刺检查”。Cruise 可以理解为有次数上限的修复导航；它负责告诉主 AI 回到哪里，但不会让审查者自己改代码，也不会绕过授权。

### 你需要确认什么

当修复需要扩大范围、启动需要授权的独立审查工具、跳过验证或执行不可逆操作时，需要你重新确认。仅修复计划内错误时，AI 可以继续推进并留下记录。

### 完成标志

失败原因有明确解释，对应修复已经验证，旧的失败结论没有被假装成新结果，任务重新回到可继续的状态。

常用检查：

```text
sdd next <项目目录>
sdd challenge <项目目录>
```

精确的 Challenge、回跳和 Cruise 规则见 [REFERENCE：Challenge](./REFERENCE.md#challenge对抗评审) 与 [REFERENCE：Cruise](./REFERENCE.md#cruise自主巡航)。

## 场景五：完成与归档

### 什么时候用

实现和验证已经完成，最终检查通过，任务准备从“正在进行”变成“已完成记录”。

### 你要提供什么

先确认结果确实符合目标。如果系统提示需要归档授权，你需要针对当前这一次归档明确同意；以前的批准、测试通过或 Challenge PASS 都不能代替这次授权。

### AI 会做什么

AI 会先检查任务文件、设计、执行记录、验证结果、Challenge 和必要的经验记录是否齐全。检查通过只表示“准备好了”，不会自动归档。

### 你需要确认什么

确认当前任务可以归档，并确认归档记录中的操作者声明和授权说明准确。这个确认只用于当前归档，不自动授权后续任务。

### 完成标志

任务及其引用的设计、执行和经验文件进入归档目录；如果以后发现问题，可以从归档任务重新开启修复，而不用重新解释全部背景。

```text
sdd validate <项目目录> --archive-ready
```

精确的授权参数、归档和 reopen 规则见 [REFERENCE：Archive / Reopen](./REFERENCE.md#archive--reopen)。

## 按需使用的高级能力

这些能力都不是开始第一个任务的前提。遇到对应场景时再启用。

### Project Profile：让 AI 先看懂已有工程

**什么时候需要：** 接手一个多包、多前后端或结构不熟悉的项目。

**它会做什么：** 只读识别工程单元、技术栈证据和命令引用，形成一份等待审阅的工程画像。

**你要确认什么：** AI 在正式确认画像前会展示精确摘要；确认动作需要当前用户针对该摘要明确授权。画像里的命令只是事实，不会被自动执行。

**完成标志：** 新任务能固定引用经过确认的工程画像，不会因为以后画像变化而重新解释历史任务。

详见 [REFERENCE：Project Engineering Profile](./REFERENCE.md#project-engineering-profile)。

### Quality Plan：解释这次应该重点验证什么

**什么时候需要：** 任务涉及多个工程单元，或你想知道验收标准与现有测试能力怎么对应。

**它会做什么：** 运行 `sdd quality plan <project-dir>`，根据当前任务和它固定引用的工程画像，生成一次性的只读说明。AC 是唯一验收真相；命令会在读取候选内容前确认它属于项目内目标，也不会绕过该边界读取候选 frontmatter。

**不会做什么：** 不创建第二套验收标准或第二套门禁，不决定通过或失败，也不修改任务。它不会初始化 Provider、安装依赖或运行验证。

详见 [REFERENCE：Quality Policy Routing](./REFERENCE.md#quality-policy-routing)。

### Verification：保存端到端验证证据

**什么时候需要：** 验收标准要求真实运行关键用户路径，而不只是单元测试。

**它会做什么：** 使用项目明确配置的验证方式运行测试，并保存独立结果。

**不会做什么：** 不自动安装浏览器或依赖，不从任务内容接收任意命令，也不会把“配置存在”当成“验证通过”。

详见 [REFERENCE：Verification Provider](./REFERENCE.md#verification-provider)。

### Visual：按设计稿或截图检查界面

**什么时候需要：** 任务明确要求视觉方向或高保真结果，并且已有经人工确认的参考材料。

**它会做什么：** AI 先根据精确 Profile / `affected-units` 判断 `ui-impact`；无法判断时只问一次。前端或混合任务用 `sdd visual select` 记录一次 `visual-context-intent`，再用 `sdd visual discover` 统一发现本地图片、文档和 URL 候选。Figma URL 与普通 URL 一样只记录为引用，不联网读取。在你明确启用后，才按静态场景和人工批准的基线生成视觉证据。

**不会做什么：** 不自动批准、创建或替换基线，不启动浏览器或执行截图 diff；没有视觉要求的任务不会因此被阻塞。

详见 [REFERENCE：Visual Context Guidance](./REFERENCE.md#visual-context-guidance按需)。

## 任务文件用来做什么

| 文件 | 用人话说 |
| :--- | :--- |
| Spec | 这次任务的目标、范围、计划、批准和最终结论。 |
| Design | 较大任务为什么选择这个方案，以及接口、数据、兼容性和验证设计。 |
| Execute Log | 实际做过的步骤、验证结果和偏差；只追加，不改写历史。 |
| Learning Record | 这次暴露出的可复用规则，帮助以后少踩同一种坑。 |

这些文件不是让用户手工填表，而是让人和 AI 在同一份事实上协作。通常由 AI 维护，你负责确认真正需要你判断的内容。

## 常用命令

| 你想做什么 | 命令 |
| :--- | :--- |
| 回到当前任务 | `sdd resume <项目目录>` |
| 看下一步 | `sdd next <项目目录>` |
| 检查当前任务 | `sdd validate <项目目录>` |
| 检查是否可归档 | `sdd validate <项目目录> --archive-ready` |
| 生成对抗检查 | `sdd challenge <项目目录>` |
| 查看所有任务 | `sdd console <项目目录>` |
| 从归档任务开启修复 | `sdd reopen <项目目录> <任务名> --defect "问题说明"` |

完整命令与精确参数见 [REFERENCE：CLI 命令](./REFERENCE.md#七cli-命令)。

## 还需要更深的信息？

- 精确字段、状态、门禁、安全边界和完整命令：[REFERENCE.md](./REFERENCE.md)
- 团队推广、角色分工和自动化：[TEAM-GUIDE.md](./TEAM-GUIDE.md)
- AI agent 的执行规则：`SKILL.md` 与 `protocols/`
- 与其他方法论的集成关系：[INTEGRATIONS.md](./INTEGRATIONS.md)

如果你只是想完成一个任务，到这里已经足够。遇到具体问题时，再沿场景中的链接查看精确参考即可。
